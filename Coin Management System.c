#include <stdio.h>
#include <stdlib.h>
#include<string.h>
#include "splash.h"

int main(void)
{
splash();
FILE*fptr;char u[50],p[50];
char user[100];
char pass[100];
char file[100];
file[100] = "pass.txt";
printf("Enter your user name:");
scanf("%c",&u);
fptr=fopen(file,"r");
fgets(user,size)


fptr=fopen("pass.txt","r");



return 0;
}
